//
// Created by benja on 18.12.2025.
//

#include "Node.h"

#include <cmath>
#include <math.h>
/*
double AddNode::evaluate() override {
    return lhs->evaluate() + rhs->evaluate();
}

double SubtractNode::evaluate() override {
    return lhs->evaluate() - rhs->evaluate();
}

double MultiplyNode::evaluate() override {
    return lhs->evaluate() * rhs->evaluate();
}

double DivideNode::evaluate() override {
    return lhs->evaluate() / rhs->evaluate();
}

double ExponentNode::evaluate() override {
    return pow(lhs->evaluate(), rhs->evaluate());
}

double VariableNode::evaluate() override {
    if (ptrValue == nullptr) {
        throw -3;
    }else {
        return *ptrValue;
    }
}

std::string VariableNode::getVariableName() {
    return this->varName;
}
*/